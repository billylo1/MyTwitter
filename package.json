{
  "name": "mytwitter",
  "private": true,
  "description": "Private family following feeds from X, hosted on Firebase",
  "scripts": {
    "oauth": "node scripts/x-oauth.cjs",
    "bootstrap": "node scripts/bootstrap-family.cjs",
    "deploy": "npx -y firebase-tools@latest deploy --project mytwitter-feed",
    "emulators": "npx -y firebase-tools@latest emulators:start --project mytwitter-feed"
  },
  "dependencies": {
    "firebase-admin": "^13.0.2",
    "twitter-api-v2": "^1.22.0"
  }
}
